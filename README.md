🔗 [Открыть проект на GitHub Pages](https://stolzie071.github.io/Web_UI_PR4/)  

# Практика 4: Создание адаптивного интерактивного интерфейса по макету

Цель — реализовать современный интерфейс (например, веб-приложение, промо-страницу и т.п.) по предоставленному или самостоятельно разработанному макету в Figma

---

## Задачи

1. **Структура и навигация**
   - Использованы семантические теги: `header`, `nav`, `main`, `section`, `article`, `footer`.
   - Реализовано меню навигации по секциям: **Локация, Маршрут, График, Галерея**.
   - Верхний хедер фиксирован, содержит логотип, основную навигацию, кнопку входа, переключатель темы и бургер-меню.

2. **Адаптивная вёрстка**
   - Подход **desktop-first** с брейкпоинтами:
     - мобильные устройства `< 768px`,
     - планшеты `768–1200px`,
     - десктопы `> 1200px`.
   - Используются **Flexbox** и **CSS Grid** для построения сеток (герой-блок, сетка галереи, двухколоночные секции).
   - На мобильных:
     - меню сворачивается в бургер,
     - часть декоративных элементов (мишки) скрывается,
     - блоки выстраиваются в одну колонку,
     - контент центрируется.
   - Отсутствует горизонтальный скролл и обрезанные элементы.

3. **Цвет и визуальный стиль**
   - Реализованы две темы: **тёмная** и **светлая**, переключение через кнопку в хедере.
   - Используются приёмы **glassmorphism**:
     - полупрозрачный фиксированный хедер с `backdrop-filter: blur(...)`;
     - модальное окно с тенью и мягкими гранями.
   - Акцентный цвет — тёплый оранжево-золотой градиент на основных CTA-кнопках.
   - Фоновая иллюстрация и изображения поддерживают тему путешествия на Камчатку.

4. **Интерактивность и анимации**
   Реализовано несколько интерактивных компонентов на JavaScript:

   - **Переключатель темы (dark / light mode)**  
     - Переключает классы `page--dark` / `page--light` на `body`.  
     - Состояние темы сохраняется в `localStorage`.

   - **Бургер-меню**  
     - На мобильных раскрывает / скрывает навигацию.
     - Закрывается по клику на пункт меню, по `Escape` и при ресайзе на десктоп.

   - **Табы «Маршрут по дням»**  
     - Реализованы на кнопках с `role="tab"` и `role="tabpanel"`.
     - При выборе дня меняется активная панель с описанием маршрута.

   - **Модальное окно бронирования тура**  
     - Открывается по кнопкам из hero-блока и секции «График туров».
     - Закрывается по крестику, клику на оверлей и по `Escape`.

   - **Галерея с лайтбоксом**  
     - При наведении карточка галереи плавно увеличивается и усиливается тень.
     - По клику изображение открывается в полноэкранном лайтбоксе, с закрытием по фону, крестику и `Escape`.

   - **Плавный скролл**  
     - Кнопка «Маршрут тура» и элементы навигации используют плавный скролл к нужной секции.

   - **Формы**  
     - Две формы (CTA + модалка бронирования) с простым JS-хендлером отправки (preventDefault + имитация отправки через alert).

5. **Доступность и этика интерфейса**
   - Использованы атрибуты:
     - `aria-label` — для кнопок (бургер, переключатель темы, закрытие модалки).
     - `role="dialog"` и `aria-modal="true"` — для модального окна и лайтбокса галереи.
     - `alt` — для всех значимых изображений (медведи, фон Камчатки, галерея, иллюстрации).
   - Для табов применены:
     - `role="tablist"`, `role="tab"`, `role="tabpanel"`,
     - `aria-selected`, `aria-controls`, `aria-labelledby`.
   - Фокус видим за счёт `outline` / тени на ключевых интерактивных элементах.
   - Контраст текста и фона подобран так, чтобы контент читался и в тёмной, и в светлой теме.
   - Формы и тексты оформлены без агрессивных паттернов, акцент на информированности и добровольности действий пользователя.

---

## Что есть в проекте

- `index.html` — основная разметка лендинга:
  - hero-блок с фоном Камчатки и описанием тура;
  - секции: **Почему именно Камчатка**, **Маршрут**, **График туров**, **Атмосфера тура**, CTA-блок и футер;
  - бургер-меню, модальное окно, лайтбокс для галереи.
- `css/style.css` — оформление:
  - переменные темной/светлой темы;
  - сетки и адаптивная вёрстка (Grid, Flexbox);
  - стили навигации, секций, карточек, форм;
  - эффекты наведения, плавные переходы, glassmorphism-элементы.
- `js/script.js` — логика интерфейса:
  - переключатель темы с `localStorage`;
  - бургер-меню и мобильная навигация;
  - табы маршрута;
  - модальное окно бронирования;
  - лайтбокс галереи;
  - плавный скролл и обработчики форм.
- `assets/images/` — изображения:
  - фон с горами Камчатки;
  - мишки;
  - иллюстрации для разделов «Почему именно Камчатка?» и «График туров»;
  - фотографии для галереи «Атмосфера тура».
- `assets/fonts/` — подключённые шрифты:
  - `AmaticSC-Bold.ttf` — для крупных заголовков;
  - `bahnschrift.ttf` — основной текст.

---

## Как смотреть

   - [Открыть проект на GitHub Pages](https://stolzie071.github.io/Web_UI_PR4/).

---

## Автор

Ларюшкин Антон, ТКМО-01-24  
Практика №4 по дисциплине  
«Тенденции проектирования графических пользовательских интерфейсов».  
Тема: «Создание адаптивного интерактивного интерфейса по макету».
